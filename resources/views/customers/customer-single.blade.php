<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Customer Profile: {{ $customer->name }}</title>
    <style>
        /** DOMPDF COMPATIBILITY SETTINGS */
        @page {
            margin: 100px 25px 60px 25px;
        }

        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            font-size: 10px;
            color: #333;
            line-height: 1.5;
        }

        /* --- HEADER & FOOTER --- */
        header {
            position: fixed;
            top: -80px;
            left: 0px;
            right: 0px;
            height: 80px;
            border-bottom: 2px solid #ea580c; /* Orange accent */
        }

        footer {
            position: fixed;
            bottom: -40px;
            left: 0px;
            right: 0px;
            height: 30px;
            border-top: 1px solid #ea580c;
            text-align: center;
            font-size: 8px;
            color: #777;
            padding-top: 5px;
        }

        .header-table {
            width: 100%;
            border-collapse: collapse;
        }

        .company-name {
            font-size: 14px;
            font-weight: bold;
            color: #111;
        }

        .company-details {
            font-size: 9px;
            color: #555;
        }

        h1 {
            font-size: 18px;
            color: #ea580c;
            margin: 0;
            text-transform: uppercase;
            font-weight: bold;
            text-align: right;
        }

        /* --- AUDIT META BOX --- */
        .meta-box {
            background-color: #fff7ed;
            border: 1px solid #fed7aa;
            padding: 10px;
            margin-bottom: 20px;
            font-size: 9px;
        }

        .meta-table {
            width: 100%;
            border-collapse: collapse;
        }

        .meta-table td {
            vertical-align: top;
        }

        /* --- SECTION STYLING --- */
        .section-title {
            font-size: 12px;
            font-weight: bold;
            color: #ea580c;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 5px;
            margin-top: 20px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        /* --- DATA TABLES --- */
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }

        .info-table th {
            text-align: left;
            width: 25%;
            padding: 6px;
            background-color: #f8fafc;
            color: #4a5568;
            border: 1px solid #e2e8f0;
            font-weight: bold;
            font-size: 9px;
        }

        .info-table td {
            padding: 6px;
            border: 1px solid #e2e8f0;
            color: #2d3748;
            font-size: 10px;
        }

        .credit-highlight {
            font-family: 'Courier New', monospace;
            font-weight: bold;
            color: #15803d; /* Green color for money/positive credit */
            font-size: 11px;
        }

        .page-number:before {
            content: "Page " counter(page);
        }
    </style>
</head>
<body>

    <header>
        <table class="header-table">
            <tr>
                <td width="60%">
                    <div class="company-name">ALPHA LOGISTICS SYSTEMS</div>
                    <div class="company-details">
                        123 Industrial Area, Enterprise Road<br>
                        Nairobi, Kenya 00100<br>
                        support@alphalogistics.com | +254 700 000 000
                    </div>
                </td>
                <td width="40%" align="right">
                    <h1>Customer Profile</h1>
                    <div class="company-details" style="margin-top: 5px;">
                        System ID: #{{ $customer->id }}
                    </div>
                </td>
            </tr>
        </table>
    </header>

    <footer>
        <table width="100%">
            <tr>
                <td align="left" width="33%">Generated via Alpha System</td>
                <td align="center" width="33%">CONFIDENTIAL DOCUMENT</td>
                <td align="right" width="33%"><span class="page-number"></span></td>
            </tr>
        </table>
    </footer>

    <main>
        <div class="meta-box">
            <table class="meta-table">
                <tr>
                    <td width="33%">
                        <strong>Generated By:</strong><br> {{ auth()->user()->name ?? 'System User' }}
                    </td>
                    <td width="33%">
                        <strong>Role:</strong><br> {{ auth()->user()->role->label ?? 'N/A' }}
                    </td>
                    <td width="33%" align="right">
                        <strong>Date:</strong><br> {{ now()->format('d M Y, h:i A') }}
                    </td>
                </tr>
            </table>
        </div>

        <div class="section-title">Customer Identity</div>
        <table class="info-table">
            <tr>
                <th>Customer Name</th>
                <td style="font-size: 12px; font-weight: bold;">{{ $customer->name }}</td>
            </tr>
            <tr>
                <th>Account ID</th>
                <td>CUST-{{ str_pad($customer->id, 5, '0', STR_PAD_LEFT) }}</td>
            </tr>
        </table>

        <div class="section-title">Contact Information</div>
        <table class="info-table">
            <tr>
                <th>Contact Number</th>
                <td>{{ $customer->number ?? '—' }}</td>
            </tr>
            <tr>
                <th>Email Address</th>
                <td>{{ $customer->email ?? '—' }}</td>
            </tr>
        </table>

        <div class="section-title">Financial Profile</div>
        <table class="info-table">
            <tr>
                <th>Credit Limit</th>
                <td class="credit-highlight">
                    KSH {{ number_format($customer->credit_limit ?? 0.00, 2) }}
                </td>
            </tr>
            <tr>
                <th>Current Status</th>
                <td>
                    {{-- Simple logic to show Active/Inactive based on recent updates or custom field --}}
                    Active
                </td>
            </tr>
        </table>

        <div class="section-title">System History</div>
        <table class="info-table">
            <tr>
                <th>Registered On</th>
                <td>{{ $customer->created_at ? $customer->created_at->format('d F Y') : '—' }} <span style="color:#777; font-size:8px;">({{ $customer->created_at?->format('H:i:s') }})</span></td>
            </tr>
            <tr>
                <th>Last Profile Update</th>
                <td>{{ $customer->updated_at ? $customer->updated_at->format('d F Y') : '—' }} <span style="color:#777; font-size:8px;">({{ $customer->updated_at?->format('H:i:s') }})</span></td>
            </tr>
        </table>

    </main>
</body>
</html>
