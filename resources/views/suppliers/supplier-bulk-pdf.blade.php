<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Supplier Master List</title>
    <style>
        /** DOMPDF COMPATIBILITY SETTINGS */
        @page {
            margin: 100px 25px 60px 25px; /* Top, Right, Bottom, Left */
        }

        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            font-size: 9px; /* Slightly smaller font to fit more columns */
            color: #333;
            line-height: 1.4;
        }

        /* --- HEADER & FOOTER --- */
        header {
            position: fixed;
            top: -80px;
            left: 0px;
            right: 0px;
            height: 80px;
            border-bottom: 2px solid #ea580c; /* Orange accent */
        }

        footer {
            position: fixed;
            bottom: -40px;
            left: 0px;
            right: 0px;
            height: 30px;
            border-top: 1px solid #ea580c;
            text-align: center;
            font-size: 8px;
            color: #777;
            padding-top: 5px;
        }

        /* --- LAYOUT TABLES --- */
        .header-table, .meta-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }

        .header-table td {
            vertical-align: middle;
        }

        /* --- TYPOGRAPHY --- */
        h1 {
            font-size: 18px;
            color: #ea580c; /* System Orange */
            margin: 0;
            text-transform: uppercase;
            font-weight: bold;
            text-align: right;
        }

        .company-name {
            font-size: 14px;
            font-weight: bold;
            color: #111;
        }

        .company-details {
            font-size: 9px;
            color: #555;
        }

        /* --- AUDIT INFO BOX --- */
        .meta-box {
            background-color: #fff7ed;
            border: 1px solid #fed7aa;
            padding: 8px;
            margin-bottom: 15px;
        }

        .meta-box strong {
            color: #ea580c;
        }

        /* --- DATA TABLE --- */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        .data-table th {
            background-color: #ea580c; /* System Orange */
            color: #ffffff;
            font-weight: bold;
            text-transform: uppercase;
            padding: 8px 5px;
            font-size: 8px;
            border: 1px solid #c2410c;
            text-align: left;
        }

        .data-table td {
            border: 1px solid #e2e8f0;
            padding: 6px 5px;
            font-size: 9px;
            color: #2d3748;
            vertical-align: top;
        }

        /* Warm/Orange Zebra Striping */
        .data-table tr:nth-child(even) {
            background-color: #ffedd5;
        }

        /* Status Colors */
        .status-active { color: #15803d; font-weight: bold; } /* Green */
        .status-inactive { color: #b91c1c; font-weight: bold; } /* Red */

        .page-number:before {
            content: "Page " counter(page);
        }
    </style>
</head>
<body>

    <header>
        <table class="header-table">
            <tr>
                <td width="60%">
                    <div class="company-name">ALPHA LOGISTICS SYSTEMS</div>
                    <div class="company-details">
                        123 Industrial Area, Enterprise Road<br>
                        Nairobi, Kenya 00100<br>
                        support@alphalogistics.com | +254 700 000 000
                    </div>
                </td>
                <td width="40%" align="right">
                    <h1>Supplier Master List</h1>
                    <div class="company-details" style="margin-top: 5px;">
                        Ref: SUP-EXP-{{ now()->timestamp }}
                    </div>
                </td>
            </tr>
        </table>
    </header>

    <footer>
        <table width="100%">
            <tr>
                <td align="left" width="33%">Generated via Alpha System</td>
                <td align="center" width="33%">CONFIDENTIAL DOCUMENT</td>
                <td align="right" width="33%"><span class="page-number"></span></td>
            </tr>
        </table>
    </footer>

    <main>
        <div class="meta-box">
            <table class="meta-table">
                <tr>
                    <td width="25%">
                        <strong>Generated By:</strong><br>
                        {{ auth()->user()->name ?? 'System User' }}
                    </td>
                    <td width="25%">
                        <strong>Role / Position:</strong><br>
                        {{ auth()->user()->role->label ?? 'N/A' }}
                    </td>
                    <td width="25%">
                        <strong>Store / Branch:</strong><br>
                        {{ auth()->user()->store->name ?? 'Head Office' }}
                    </td>
                    <td width="25%" align="right">
                        <strong>Generated On:</strong><br>
                        {{ now()->format('d M Y, h:i A') }}
                    </td>
                </tr>
            </table>
        </div>

        <table class="data-table">
            <thead>
                <tr>
                    <th width="5%">#</th>
                    <th width="20%">Supplier Name</th>
                    <th width="15%">Contact Person</th>
                    <th width="15%">Email</th>
                    <th width="10%">Phone</th>
                    <th width="20%">Address</th>
                    <th width="5%">Status</th>
                    <th width="10%">Registered</th>
                </tr>
            </thead>
            <tbody>
                @foreach($suppliers as $supplier)
                    <tr>
                        <td align="center">{{ $loop->iteration }}</td>
                        <td style="font-weight: bold;">{{ $supplier->name ?? '—' }}</td>
                        <td>{{ $supplier->contact_person ?? '—' }}</td>
                        <td>{{ $supplier->email ?? '—' }}</td>
                        <td>{{ $supplier->phone ?? '—' }}</td>

                        {{-- Truncate long addresses --}}
                        <td>{{ \Illuminate\Support\Str::limit($supplier->address, 40) ?? '—' }}</td>

                        {{-- 🟢 Status Color Logic --}}
                        <td>
                            @if($supplier->is_active)
                                <span class="status-active">Yes</span>
                            @else
                                <span class="status-inactive">No</span>
                            @endif
                        </td>

                        <td>{{ $supplier->created_at ? $supplier->created_at->format('d M Y') : '—' }}</td>
                    </tr>
                @endforeach
            </tbody>
        </table>

        <div style="margin-top: 20px; text-align: right; font-size: 9px; color: #777; border-top: 1px dashed #ea580c; padding-top: 5px;">
            <strong>Total Suppliers Listed:</strong> {{ count($suppliers) }}
        </div>
    </main>

</body>
</html>
