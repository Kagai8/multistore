<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Store Profile: {{ $store->name }}</title>
    <style>
        /** DOMPDF COMPATIBILITY SETTINGS */
        @page {
            margin: 100px 25px 60px 25px;
        }

        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            font-size: 10px;
            color: #333;
            line-height: 1.5;
        }

        /* --- HEADER & FOOTER --- */
        header {
            position: fixed;
            top: -80px;
            left: 0px;
            right: 0px;
            height: 80px;
            border-bottom: 2px solid #ea580c; /* Orange accent */
        }

        footer {
            position: fixed;
            bottom: -40px;
            left: 0px;
            right: 0px;
            height: 30px;
            border-top: 1px solid #ea580c;
            text-align: center;
            font-size: 8px;
            color: #777;
            padding-top: 5px;
        }

        .header-table {
            width: 100%;
            border-collapse: collapse;
        }

        .company-name {
            font-size: 14px;
            font-weight: bold;
            color: #111;
        }

        .company-details {
            font-size: 9px;
            color: #555;
        }

        h1 {
            font-size: 18px;
            color: #ea580c;
            margin: 0;
            text-transform: uppercase;
            font-weight: bold;
            text-align: right;
        }

        /* --- AUDIT META BOX --- */
        .meta-box {
            background-color: #fff7ed;
            border: 1px solid #fed7aa;
            padding: 10px;
            margin-bottom: 20px;
            font-size: 9px;
        }

        .meta-table {
            width: 100%;
            border-collapse: collapse;
        }

        .meta-table td {
            vertical-align: top;
        }

        /* --- SECTION STYLING --- */
        .section-title {
            font-size: 12px;
            font-weight: bold;
            color: #ea580c;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 5px;
            margin-top: 20px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        /* --- DATA TABLES --- */
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }

        .info-table th {
            text-align: left;
            width: 25%;
            padding: 6px;
            background-color: #f8fafc;
            color: #4a5568;
            border: 1px solid #e2e8f0;
            font-weight: bold;
            font-size: 9px;
        }

        .info-table td {
            padding: 6px;
            border: 1px solid #e2e8f0;
            color: #2d3748;
            font-size: 10px;
        }

        .code-highlight {
            font-family: 'Courier New', monospace;
            font-weight: bold;
            color: #ea580c;
            font-size: 11px;
        }

        .page-number:before {
            content: "Page " counter(page);
        }
    </style>
</head>
<body>

    <header>
        <table class="header-table">
            <tr>
                <td width="60%">
                    <div class="company-name">ALPHA LOGISTICS SYSTEMS</div>
                    <div class="company-details">
                        123 Industrial Area, Enterprise Road<br>
                        Nairobi, Kenya 00100<br>
                        support@alphalogistics.com | +254 700 000 000
                    </div>
                </td>
                <td width="40%" align="right">
                    <h1>Store Profile</h1>
                    <div class="company-details" style="margin-top: 5px;">
                        Ref: {{ $store->code }}
                    </div>
                </td>
            </tr>
        </table>
    </header>

    <footer>
        <table width="100%">
            <tr>
                <td align="left" width="33%">Generated via Alpha System</td>
                <td align="center" width="33%">CONFIDENTIAL DOCUMENT</td>
                <td align="right" width="33%"><span class="page-number"></span></td>
            </tr>
        </table>
    </footer>

    <main>
        <div class="meta-box">
            <table class="meta-table">
                <tr>
                    <td width="33%">
                        <strong>Generated By:</strong><br> {{ auth()->user()->name ?? 'System User' }}
                    </td>
                    <td width="33%">
                        <strong>Role:</strong><br> {{ auth()->user()->role->label ?? 'N/A' }}
                    </td>
                    <td width="33%" align="right">
                        <strong>Date:</strong><br> {{ now()->format('d M Y, h:i A') }}
                    </td>
                </tr>
            </table>
        </div>

        <div class="section-title">Store Identity</div>
        <table class="info-table">
            <tr>
                <th>Store Name</th>
                <td style="font-size: 12px; font-weight: bold;">{{ $store->name }}</td>
            </tr>
            <tr>
                <th>Store Code</th>
                <td class="code-highlight">{{ $store->code }}</td>
            </tr>
            <tr>
                <th>System ID</th>
                <td>#{{ $store->id }}</td>
            </tr>
            <tr>
                <th>Type / Category</th>
                <td>{{ $store->type ?? 'General Retail' }}</td>
            </tr>
        </table>

        <div class="section-title">Contact Information</div>
        <table class="info-table">
            <tr>
                <th>Phone Number</th>
                <td>{{ $store->phone ?? '—' }}</td>
            </tr>
            <tr>
                <th>Email Address</th>
                <td>{{ $store->email ?? '—' }}</td>
            </tr>
            <tr>
                <th>Physical Address</th>
                <td>{!! nl2br(e($store->address ?? 'No address provided')) !!}</td>
            </tr>
        </table>

        <div class="section-title">System Metadata</div>
        <table class="info-table">
            <tr>
                <th>Created On</th>
                <td>{{ $store->created_at ? $store->created_at->format('d F Y') : '—' }} <span style="color:#777; font-size:8px;">({{ $store->created_at?->format('H:i:s') }})</span></td>
            </tr>
            <tr>
                <th>Last Updated</th>
                <td>{{ $store->updated_at ? $store->updated_at->format('d F Y') : '—' }} <span style="color:#777; font-size:8px;">({{ $store->updated_at?->format('H:i:s') }})</span></td>
            </tr>
        </table>

    </main>
</body>
</html>
